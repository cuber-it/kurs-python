# Aufgabe (basic): Temperaturumrechnung als Funktion
#
# Schreibe eine Funktion celsius_nach_fahrenheit(c), die Grad Celsius
# in Grad Fahrenheit umrechnet und das Ergebnis ZURÜCKGIBT (return!).
#
# Formel:  fahrenheit = celsius * 9/5 + 32
#
# Schritt 1: Funktion mit einem Parameter c definieren.
# Schritt 2: Ergebnis mit return zurückgeben (NICHT print in der Funktion!).
# Schritt 3: Funktion für mehrere Werte aufrufen und die Rückgabe ausgeben.
#
# Bonus: zweite Funktion fahrenheit_nach_celsius(f) für die Rückrichtung.

# V (Verarbeitung – die Funktion)
# def celsius_nach_fahrenheit(c):
#     ...
#     return ...

# A (Ausgabe – Aufrufe)
# for c in [0, 20, 37, 100]:
#     print(f"{c} °C = ... °F")
# ...
